#include <stdio.h>
int main() {
  for (int i=0;i<n;i++){
    break;
  }
   count++;
}
if(idx!=-1){
    printf("the element %d is int the array at index %d, target,idx);
} 
else{
    printf("the element %d is not  in the array at index %d, target,idx)");
} 
     
    return 0;
}
