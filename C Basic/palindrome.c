#include <stdio.h>
int main() {
    int n,temp,rem=0,rev=0;
   printf("enter a number:");
   scanf("%d",&n);
    n=temp;
    while(temp!=0){
        rem=temp%10;
        rev=rev*10+rem;
        temp=temp/10;
     {
     }
     printf("%d",rev);
     if(n==rev);
     printf("palindrome");
     else
     printf("not palindrome");
    return 0;
}
    
}
  
