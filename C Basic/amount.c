// Online C compiler to run C program online
#include <stdio.h>
int main()  {
    int account;
    scanf("%d",&account);
    if(account>8)
    printf("account locked");
    else
    printf("login allowed");
    return 0;
    
}
