#include <stdio.h>
int main() {
   int equlibrium index (int arr[], int n);
   int total;
   int left;
   for(int i=0;i<n;i++){
    total+= arr[i];
   for(int i=0;i<n;i++){
    total-= arr[i];
   if(left== total){
     return i;
   }
   left += arr[i];
   }
   return -1;
   }
   int main()
   int n;
   printf("enter number of elements:");
   scanf("%d",n);
    
    
    return 0;
}
