// Online C compiler to run C program online
#include <stdio.h>
int main()  {
    int speed;
    scanf("%d",&speed);
    if(speed>8)
    printf("speed limit exeded");
    else
    printf("with speed limit");
    return 0;
    
}
