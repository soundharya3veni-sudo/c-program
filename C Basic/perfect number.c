int main() {
   int i,n,sum=0;rem=0;
   printf("enter a number:");
   scanf("%d",&n);
    int t#include <stdio.h>
emp=n;
     for(i=1;i<n;i++ ){
        rem=n%i;
        if(rem==0){
            sum=sum+i;

        }
     }
     if(sum==temp);
     printf("perfect number");
     else
     printf("not perfect number");
    return 0;
}
